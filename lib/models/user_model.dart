class User {
  String? uid;
  String? email;
  String? first_name;
  String? second_name;
  String? password;

  UserModel(
      {this.uid, this.email, this.first_name, this.second_name, this.password});
}
